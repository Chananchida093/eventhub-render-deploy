services:
  - type: web
    name: gather-event-registration-v2
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/events
    autoDeployTrigger: commit
